// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your app, use the WidgetTester
// utility in the flutter_test package. For example, send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launch test', (WidgetTester tester) async {
    // Basic test to verify the test framework is working
    expect(1 + 1, 2);
  });
}
